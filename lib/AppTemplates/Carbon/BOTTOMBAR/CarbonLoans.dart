import 'package:flutter/material.dart';

class CarbonLoans extends StatelessWidget {
  const CarbonLoans({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("CarbonLoans"),
      ),
    );
  }
}
